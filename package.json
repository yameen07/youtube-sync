{
  "name": "youtube-sync",
  "version": "1.0.0",
  "description": "YouTube playback synchronization app",
  "private": true,
  "scripts": {
    "build": "npm install --prefix server --ignore-scripts && npm install --prefix client && npm run build --prefix client",
    "start": "node server/server.js",
    "install:all": "npm install --prefix server && npm install --prefix client"
  },
  "engines": {
    "node": ">=14.0.0"
  }
}
