{
  "name": "youtube-sync-server",
  "version": "1.0.0",
  "description": "WebSocket server for YouTube playback synchronization",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "build": "cd ../client && npm install && npm run build",
    "postinstall": "npm run build"
  },
  "keywords": [
    "websocket",
    "youtube",
    "sync"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "ws": "^8.14.2"
  },
  "engines": {
    "node": ">=14.0.0"
  }
}
